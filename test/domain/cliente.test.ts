

describe('Cliente Domain tests', () => {
    it('should create a cliente instance', () => {

    })

    it('should thorow error when creating a cliente with invalid data', () => {

    })

    it('should update cliente information', () => {
        
    })
})